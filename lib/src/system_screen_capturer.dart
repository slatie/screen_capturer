class SystemScreenCapturer {
  Future<void> capture({
    required String imagePath,
    bool silent = true,
    int xStart = -1,
    int yStart = -1,
    int xEnd = -1,
    int yEnd = -1,
  }) {
    throw UnimplementedError();
  }
}
